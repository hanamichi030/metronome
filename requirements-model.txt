# Extra dependencies for YOUR model only.
# Keep these out of the upstream requirements.txt so `git merge upstream/main`
# never conflicts on the dependency list.
#
# Install with:  pip install -r requirements-model.txt
#
# The served model (poker44_model/model.joblib) is a rank-fused ensemble whose
# members require ALL of the following. A missing/mismatched dep makes
# joblib.load() throw -> score_batch() returns the 0.5 fallback -> reward 0.
#
#   * scikit-learn: the artifact was pickled with 1.9.0 -- pin it EXACTLY, or
#     unpickling is not guaranteed (InconsistentVersionWarning / invalid results).
#     This overrides the looser `scikit-learn>=1.3` in requirements.txt.
#   * lightgbm: the stack and monotone members are LightGBM.
#   * xgboost: the stack contains an XGBClassifier. MUST stay < 3.0 -- a pickled
#     XGBClassifier fails to load on xgboost 3.x ("input stream corrupted").
# The MLP member is sklearn's MLPClassifier (NOT torch), so torch is not needed.
scikit-learn==1.9.0
lightgbm>=4.0
xgboost>=2.0,<3.0
